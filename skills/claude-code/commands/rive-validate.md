# Rive Validate

Validate a Rive .riv binary file for structural correctness.

Usage: /rive-validate <file.riv>

Steps:
1. Run: cargo run -- validate <file.riv>
2. Report validation result (pass/fail)
3. If validation fails, show the error details
4. Suggest fixes based on common issues
