name: Implement stacking order

on:
  push:
    branches:
      - agent/authoring-stacking-order

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/authoring-stacking-order
      - run: echo staged
