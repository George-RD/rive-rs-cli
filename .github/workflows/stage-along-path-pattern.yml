name: Stage along-path pattern

on:
  push:
    branches:
      - agent/along-path-pattern-v0

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  stage:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/along-path-pattern-v0
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - run: python3 scripts/stage_along_path_pattern.py
      - run: python3 scripts/fix_along_path_stage.py
      - run: cargo fmt --all
      - name: Regenerate the published AuthoringSpec schema
        run: cargo test --locked --all-features --test authoring_contract regenerate_published_authoring_schema -- --ignored
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo test --locked --all-features --test authoring_along_path_contract
      - run: cargo test --locked --all-features --test authoring_contract published_authoring_schema_matches_generated_contract
      - run: cargo test --locked --all-features
      - name: Commit the verified implementation without staging machinery
        run: |
          rm .github/workflows/stage-along-path-pattern.yml
          rm scripts/stage_along_path_pattern.py
          rm scripts/fix_along_path_stage.py
          git diff --check
          test ! -e .github/workflows/stage-along-path-pattern.yml
          test ! -e scripts/stage_along_path_pattern.py
          test ! -e scripts/fix_along_path_stage.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: add deterministic along-path patterns"
          git push origin HEAD:agent/along-path-pattern-v0
