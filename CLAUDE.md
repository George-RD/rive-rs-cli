# CLAUDE.md

Read `AGENTS.md` and `src/objects/AGENTS.md` for all project knowledge, conventions, anti-patterns, and coding rules.

## Agent skills

Matt Pocock engineering skills are vendored under `.agents/skills/` alongside the existing project skills.

### Issue tracker

Specs and implementation tickets are tracked in GitHub Issues for `George-RD/rive-rs-cli`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo. Read `CONTEXT.md`; use Cairn decisions/contracts/todos as the durable architecture and status authority. See `docs/agents/domain.md`.
